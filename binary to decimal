function decimal(n){
    let i=0
    let ans=0
    while(n!=0){
        let digit=n%10
        if(digit==1){
            ans+=Math.pow(2,i)
        }
        n=n/10
        i++
    }

return ans
}
let a=decimal(1010)
console.log(a);